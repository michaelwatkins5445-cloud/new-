services:
  - type: web
    name: profile-api
    env: node
    plan: free
    branch: main
    buildCommand: npm run render-build
    startCommand: npm run render-start
    envVars:
      - key: NODE_VERSION
        value: 18
